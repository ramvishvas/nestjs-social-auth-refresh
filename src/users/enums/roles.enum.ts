export enum RolesEnum {
  ADMIN = 'admin',
  USER = 'user',
  MANAGER = 'manager', // Optional, if your application needs a manager role
  GUEST = 'guest', // Optional, if your application needs a guest role
}
